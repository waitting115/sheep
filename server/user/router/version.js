// 改变版本：post user/version/standard  user/version/all  (openId获取方式？)

const versionStandard = (req, res) => {
}

const versionAll = (req, res) => {
}

module.exports = {
    versionStandard,
    versionAll
};