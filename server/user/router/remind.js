// 添加/删除提醒：post user/addRemind/{id} user/removeRemind/{id}  (openId获取方式？)

const addRemind = (req, res) => {
}

const removeRemind = (req, res) => {
}

module.exports = {
    addRemind,
    removeRemind
};